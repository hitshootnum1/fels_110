<%= form_for(:relationships, url: user_relationship_path(
  current_user.active_relationships.find_by(followed_id: @user.id)),
  html: { method: :delete},
  remote: true) do |f| %>
  <%= f.submit t("user.unfollow"), class: "btn" %>
<% end %>
